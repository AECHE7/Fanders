[phases.setup]
nixPkgs = ["...", "php82", "php82Extensions.pdo_pgsql", "php82Extensions.mbstring", "php82Extensions.session"]

[phases.install]
cmds = ["composer install --no-dev --optimize-autoloader"]

[phases.build]
cmds = ["echo 'PHP app ready for deployment'"]

[start]
cmd = "php -S 0.0.0.0:$PORT -t public/"
